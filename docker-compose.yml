services:
  gateway:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_HOST=${OLLAMA_HOST:-}
  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    profiles: ["local-models"]
